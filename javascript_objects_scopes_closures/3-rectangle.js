#!/usr/bin/node
// Node.js script that defines a rectangle
class Rectangle {
  constructor (w, h) {
    if (w > 0 && h > 0) {
      this.width = w;
      this.height = h;
    }
    print() {
      for (let x = 0; x < this.height; x++) {
        
      }
    }
  }
}
module.exports = Rectangle;
